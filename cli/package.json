{
  "name": "@agent-hero/agent-os",
  "version": "0.1.0",
  "description": "Agent-OS CLI — manage AI agents locally or in the cloud",
  "bin": {
    "agent-os": "./bin/agent-os.js"
  },
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "echo 'No build step needed'",
    "test": "node --test test/"
  },
  "keywords": ["ai", "agents", "agent-os", "cli"],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
